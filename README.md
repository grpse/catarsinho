# README

## dependencies
    - docker
    - docker-compose

## install
    - docker-compose run --rm app rails new . --force --database=mysql --skip-bundle
    - docker-compose build

## run
    - docker-compose up

